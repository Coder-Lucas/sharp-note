import * as react from "react";
import styles from "#/header/header.module.scss";

const HeaderSlider: react.FC = () => {
    return (<>
        <aside className={styles.headerSlider}></aside>
    </>);
};

export default HeaderSlider;
